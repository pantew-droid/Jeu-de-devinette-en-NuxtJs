export default defineAppConfig({
  ui: {
    colors: {
      primary: 'green',
      neutral: 'slate'
    },
    notifications: {
      // Affiche les toasts en haut à droite de l'écran
      position: 'top-0 bottom-[unset]'
    }
  }
})
